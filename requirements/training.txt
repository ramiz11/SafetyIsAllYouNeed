numpy<2.0
pandas==2.2.2
tqdm>=4.66.2
safetensors>=0.4.3
transformers>=4.41.0
accelerate>=0.30.0
peft>=0.11.1
datasets>=2.20.0
huggingface_hub>=0.23.0
# 4-bit quantization (CUDA/Linux). Skip on Windows/Mac if unsupported.
bitsandbytes>=0.43.0; platform_system != "Windows"
# IMPORTANT: install torch separately for your platform/wheel:
#   CUDA 12.1 example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   (Or choose the matching cuXXX / cpu index as needed.)
